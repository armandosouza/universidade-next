const size = {
	mobileMax: "425px",
	mobileLMax: "768px",
	tabletMax: "1024px",
}

export const device = {
	mobileMax: `(max-width: ${mobileMax})`,
	mobileLMax: `(max-width: ${mobileLMax})`,
	tabletMax: `(max-width: ${tabletMax})`
}